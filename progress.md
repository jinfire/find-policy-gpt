# 현재 진행 상황

기준일: 2026-08-01

## 실행 상태

- localhost: http://localhost:3002
- GitHub `main`: 기능 단위 커밋과 푸시 완료
- 외부 공개 배포는 사용하지 않음
- 정부24 전체 카탈로그 실제 적재 건수: **0건** — API 키 입력 후 최초 동기화 필요

## 완료한 사용자 기능

- 중복 질문을 줄인 3단계 사용자 정보 입력
- 만 나이, 혼인기간, 자녀 월령, 수도권, 가구원 수, 합산소득 자동 파생
- 2026년 보건복지부 기준으로 예상 기준 중위소득 비율 실시간 표시
- 검수된 10개 정책의 규칙 기반 정밀 추천
- 각 정책의 추천 이유, 추가 확인 질문, 혜택, 공식 링크 제공
- 사용자 결과에서 `가능성 낮은 정책` 완전 제외
- 정부24 전체 1만여 개 혜택을 위한 원문 검색 UI와 D1 검색 API
- 비회원 입력값은 브라우저 메모리에서만 사용
- 모바일 반응형과 reduced-motion 대응

## 완료한 전체 정책 기반

- 정부24 `serviceList`, `serviceDetail`, `supportConditions` 전 페이지 수집 클라이언트
- 세 응답을 공식 `서비스ID`로 병합하는 정규화기
- 중앙부처·지자체·공공기관·교육청 원본을 보존하는 `source_catalog_services`
- 이름만으로 합치지 않는 원본→검수정책 매핑 구조
- 내용 해시, 최초·마지막 발견일, 활성/비활성, 동기화 실행 이력
- 멱등 D1 upsert SQL과 사라진 정책의 소프트 비활성화
- `.env.local` 키 보호, 로컬 스냅샷·SQL Git 제외
- `npm run catalog:sync:local` 한 번으로 수집→정규화→로컬 D1 적재
- 전체 원본은 `search_only`, 사람 검수 정책은 `rule_ready`로 분리

## DB와 운영

- D1/SQLite + Drizzle 마이그레이션
- 총 15개 테이블: 원본 카탈로그 4, 검수 정책·운영 7, 사용자·매칭 4
- 10개 정밀 정책 seed와 버전·출처 데이터
- 매년 6월·12월 정기 검토 규칙
- 직전 완료 검토월을 기준으로 하는 출처 최신성 검사
- GitHub Actions의 push/PR 및 6월 1일·12월 1일 예약 품질 검사
- 공식 링크 검사, 오류 제보, 변경 이벤트 구조

## 품질 확인

- 12개 테스트 파일, 33개 테스트 통과
- ESLint 통과
- TypeScript `--noEmit` 통과
- vinext 프로덕션 빌드 통과
- D1 빈 DB 전체 마이그레이션 테스트 통과
- 정부24 계약·정규화·멱등 upsert 테스트 통과
- localhost 홈과 `/api/catalog` HTTP 200 확인

## 이번 작업의 기능별 원격 커밋

- `c9dac87` — 2026 예상 기준 중위소득 표시
- `dd6ae78` — 가능성 낮은 정책 사용자 노출 제거
- `a10173f` — 전국 정부24 카탈로그 수집·DB 파이프라인
- `ba2d615` — 전체 혜택 검색 API·화면
- `ded8d75` — 6월·12월 정기 검토 규칙

## 현재 범위의 정확한 의미

앱은 10개 정책을 `rule_ready` 수준으로 정밀 추천한다. 전체 1만여 건은 검색·변경 추적이 가능한 `search_only` 기반까지 구현했으며, 실제 레코드 적재에는 공공데이터포털 인증키가 필요하다. 전체 1만여 건을 모두 자동 자격 판정하는 상태는 아니다.
