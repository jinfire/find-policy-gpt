# 현재 진행 상황

기준일: 2026-08-03

## 실행 상태

- localhost: http://localhost:3002
- GitHub `main`: 기능 단위 커밋과 푸시 완료
- 외부 공개 배포는 사용하지 않음
- 정부24 로컬 D1 실제 적재: **10,964건**, 전부 활성·`partially_structured`
- 지역 분류: 전국 1,655건, 지역 9,309건
- 연령 또는 중위소득 구조화 조건이 있는 정책: 10,957건

## 사용자에게 제공되는 결과

1. 공식 원문을 직접 검수한 10개 `rule_ready` 정책을 정밀 룰로 판정한다.
2. 같은 입력에서 파생한 값으로 정부24 10,964건을 다시 비교한다.
3. 연령·성별·소득 구간처럼 확인된 필수 조건이 맞지 않는 정책은 제외한다.
4. 장애·한부모·질환 등 아직 입력하지 않은 조건은 탈락시키지 않고 추가 확인으로 표시한다.
5. 전국 정책과 사용자의 시·도에 해당하는 지역 정책만 후보로 남긴다.
6. 전체 후보 수와 조건 일치도 상위 50건을 추천 이유와 함께 보여준다.
7. `가능성 낮은 정책`은 결과 화면에 표시하지 않는다.

## 완료한 기능

- 중복 질문을 줄인 3단계 사용자 입력과 직접 가구원 수 입력
- 만 나이, 혼인기간, 자녀 수·월령, 합산소득, 2026년 예상 기준 중위소득 자동 파생
- 결과 화면에서만 예상 기준 중위소득 비율 표시
- 검수된 10개 정책의 3값 룰 기반 정밀 추천
- 정부24 목록·상세·지원조건 전 페이지 수집과 서비스 ID 병합
- 공식 지원조건 48개 코드를 8개 조건군과 연령 범위로 정규화
- 10,964개 전체 정책의 1차 조건 후보 생성·정렬 API
- 후보별 추천 이유, 추가 확인 조건, 지원 내용, 공식 링크
- 중앙/지역 기관 유형 교정과 시·도 기준 지역 후보 필터
- 전체 원문 검색 UI와 D1 검색 API
- 긴 정부 원문을 카드용 길이로 제한
- 원본 생년월일·소득액 대신 매칭용 파생값만 서버에 전송하며 저장하지 않음
- 같은 동기화 파일을 다시 적용할 수 있는 멱등 적재
- 6월·12월 정기 검토 규칙과 예약 품질 검사

## DB 상태

- D1/SQLite + Drizzle, 총 15개 테이블
- `source_catalog_services.eligibility_profile`에 조건군 JSON 저장
- 전체 정부24 원본: `partially_structured`
- 사람 검수와 경계값 테스트를 마친 10개: `rule_ready`
- 원본 JSON, 내용 해시, 최초·마지막 발견일, 활성 상태, 동기화 이력 보존
- `.env.local`, `.local`, `.wrangler`는 Git 제외

## 현재 정확도의 의미

- 10개 `rule_ready` 정책은 정책별 세부 조건 트리로 판정한다.
- 10,964개 `partially_structured` 정책은 정부24의 구조화 코드로 **1차 후보**를 만든다.
- 구조화 코드에 없는 거주기간, 세부 재산, 특정 질환, 공고 기간 등은 자동 확정하지 않는다.
- 전체 후보에는 항상 공식 원문의 세부 선정기준을 다시 확인하라는 안내를 붙인다.
- 기업·기관 대상 정책은 현재 개인용 입력 흐름에서 제외한다.

## 이번 작업의 기능별 원격 커밋

- `fad498a` — 정부24 구조화 조건 해석·판정 엔진
- `d4063b6` — 전체 정책 eligibility profile DB 저장과 마이그레이션
- `84d3e9a` — 전체 후보 필터·추천 이유·정렬 서비스
- `8bdfe02` — 전체 추천 API와 결과 화면 연결, 지역 분류 교정

## 검증

- TDD: 각 기능을 실패 테스트부터 작성한 뒤 구현
- 14개 테스트 파일, 46개 테스트 통과
- TypeScript `--noEmit` 통과
- ESLint 통과
- vinext 프로덕션 빌드 통과
- 검수 정책 공식 링크 10개 HTTP 200, 2026년 6월 최신성 기준 통과
- D1 마이그레이션·멱등 재적재 테스트 통과
- localhost 추천 API: 10,964건 조회 및 상위 50건 반환 확인
- 브라우저에서 3단계 입력 → 중위소득 → 정밀 정책 → 전체 정책 후보 → 공식 링크 렌더링 확인
