# 현재 진행 상황

기준일: 2026-08-04

## 실행 상태

- localhost: http://localhost:3002
- GitHub `main`: 기능 단위 커밋과 푸시 완료
- 외부 공개 배포는 사용하지 않음
- 정부24 로컬 D1: 활성 정책 10,964건, 전부 `partially_structured`
- 사람 검수 POC: 10개 `rule_ready`

## 현재 사용자 흐름

1. 본인 정보와 시·도/시·군·구를 입력한다.
2. 가구원 수, 총 자녀 수, 입양·임신, 소득을 입력한다.
3. 현재 직업·소속을 하나 선택한다. 사립학교 교직원일 때만 사학연금을 추가로 묻는다.
4. 장애·한부모·다문화·보훈 등 특수조건과 주택 수를 선택한다.
5. 브라우저에서 만 나이와 예상 기준 중위소득 비율을 계산한다.
6. POC 10개와 정부24 전체 카탈로그를 같은 조건으로 비교해 하나의 `맞춤 추천 정책` 목록으로 보여준다.

## 현재 추천 기준

- 10개 POC와 10,964건 카탈로그를 사용자 화면에서 구분하지 않는다.
- 확실히 불일치하는 정책은 제외한다.
- 사용자 답변과 일치하는 추천 근거가 없는 정책은 제외한다.
- 연령·소득·성별 하나만 넓게 맞는 정책도 제외한다.
- 자녀 수, 입양, 직업, 무주택, 특수조건처럼 구체적인 일치 근거는 카드에 설명한다.
- 원문의 `월평균보수 N만원 미만/이하` 조건은 본인 연소득의 월 환산값과 비교한다.
- `가능성 낮은 정책`은 표시하지 않는다.
- 실제 선정 여부는 공식 기관 심사에 따르므로 공식 원문 확인 안내를 유지한다.

## 이번에 완료한 기능

- 10,964건 원문 키워드 분포 분석과 질문 재설계 문서
- 가구원 수와 총 자녀 수 분리, 출생 순위 질문 제거
- 전국 17개 시·도와 전체 시군구 선택
- 시군구 이름뿐 아니라 시·도까지 일치하는 지역 필터
- 입양·임신·직업·소속·특수가구 4단계 질문
- 교직원 전용 정책에서 일반 근로자 제외
- 사립학교 교직원에게만 사학연금 가입 여부 질문
- 3자녀 이상 원문 조건에서 자녀 2명 제외
- 직접 검토 10개와 전체 카탈로그 결과 통합·정렬·이름 중복 제거
- 추천 근거가 없거나 넓은 인구통계 근거 하나뿐인 후보 제거
- `사회보험사각지대해소` 등 월평균보수 상한 정책의 소득 필터
- 중위소득은 결과 화면에서만 표시

## DB 상태

- D1/SQLite + Drizzle, 총 15개 테이블
- `source_catalog_services.eligibility_profile`에 정부24 구조화 조건군 JSON 저장
- 원문 JSON, 내용 해시, 최초·마지막 발견일, 활성 상태, 동기화 이력 보존
- 자녀 수·입양·교직원·거주기간 등 명확한 텍스트 조건은 현재 요청 시 원문에서 추출
- 이번 질문·필터 변경에는 DB 마이그레이션 없음
- `.env.local`, `.local`, `.wrangler`는 Git 제외

## 정확도의 의미

- 10개 `rule_ready`는 정책별 세부 조건 트리로 판정한다.
- 10,964개 `partially_structured`는 정부24 구조화 조건과 안전하게 추출 가능한 원문 조건으로 판정한다.
- 모든 원문의 세부 재산, 특정 질환, 신청기간, 예외 조항까지 구조화한 상태는 아니다.
- 기업·기관 대상 정책은 개인용 추천 흐름에서 제외한다.

## 기능별 원격 커밋

- `0ba0333` — 10,964건 분석 기반 질문 재설계 문서
- `80f5079` — 자녀·입양·직업·사학연금·시군구 명시적 필터
- `154ce80` — 4단계 질문 화면과 단일 추천 목록

## 검증

- TDD: 실패 테스트 작성 후 구현
- 14개 테스트 파일, 58개 테스트 통과
- TypeScript `--noEmit` 통과
- ESLint 통과
- vinext 프로덕션 빌드 통과
- 로컬 D1 10,964건 대상 예시 프로필 실측: 추천 후보 58건
- localhost:3002 유지, 외부 배포 없음
